language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install pytest pytest-cov
# command to run tests
script:
  - py.test --cov=. .
after:
  - coveralls
